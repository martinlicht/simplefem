name: C/C++ CI

on:
  schedule:
    0 4 * * *
#  push:
#    branches: [ develop ]
#  pull_request:
#    branches: [ develop ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: checkout repository
      uses: actions/checkout@v2
    - name: build project
      run: make
#    - name: make cpplint
#      run: make cpplint
